import { Button, StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function L44Counter() {
  return (
    <View>
        <Button title='increment' onPress={()=>{}}/>
        <Button title='decrement' onPress={()=>{}}/>
    </View>
  )
}

const styles = StyleSheet.create({})